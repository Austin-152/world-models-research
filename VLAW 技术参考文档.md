# VLAW 技术参考文档

**论文全称：** VLAW: Iterative Co-Improvement of Vision-Language-Action Policy and World Model  
**作者：** Yanjiang Guo, Tony Lee, Lucy Xiaoyang Shi, Jianyu Chen, Percy Liang, Chelsea Finn  
**arXiv：** [2602.12063](https://arxiv.org/abs/2602.12063)

---

## 一、论文定位（读前必看）

VLAW 解决的核心问题是：**真实机器人上收集 policy rollout 成本极高，现有 action-conditioned 世界模型又缺乏足够物理保真度，无法直接用于策略改进**。论文提出一种迭代协同改进框架：用少量真实 rollout 微调世界模型，再用微调后的世界模型在"想象"中生成大规模合成轨迹，配合 VLM 奖励模型筛选成功轨迹，以监督式 flow-matching 目标改进 VLA 策略。

在世界模型领域，VLAW 处于 **"世界模型 + 策略学习闭环"** 的前沿实践位置——不是单纯训练预测模型，而是把世界模型当作可扩展的数据生成器，嵌入真实机器人后训练管线。与 VITRA（用人类视频预训练 VLA 基座）互补：VITRA 解决"冷启动数据从哪来"，VLAW 解决"有了基座后如何用世界模型低成本迭代改进"。

- **与团队目标"世界模型基座建立"的关联度：高**
  - 直接给出世界模型在真实机器人场景下的落地范式：在线 rollout 接地（grounding）、合成数据生成、与 VLA 策略的迭代共进化
  - 明确指出当前世界模型的两大工程痛点（过度乐观、接触/形变建模差），并给出可操作的缓解方案
  - 将世界模型训练目标（扩散去噪）与策略训练目标（flow-matching）统一到加权回归框架，便于工程复现

---

## 二、核心运作原理

### 核心思路（工程类比）

可以把它理解成 **"双模型互相喂数据的迭代蒸馏循环"**：

1. 策略在真实世界跑一小批轨迹（含大量失败案例）→ 给世界模型"补课"，让它学会真实物理交互（不只是演示数据里的成功轨迹）
2. 补课后的世界模型充当 **低成本仿真器**，让策略在想象中跑成千上万条轨迹
3. VLM 奖励模型当 **自动判卷老师**，只保留"想象里做对了"的轨迹
4. 用这些合成成功轨迹 + 真实成功轨迹，以加权 flow-matching 微调策略
5. 回到第 1 步，重复迭代

类比：像一个不断用真实考试错题（online rollout）校准模拟器（world model），再用校准后的模拟器做大规模模拟考（synthetic rollout），最后只把模拟考里做对的题拿来训练学生（VLA policy）。

### 主要模块

| 模块                    | 职责                                                         |
| ----------------------- | ------------------------------------------------------------ |
| **VLA 策略 π_θ**        | 基于 π₀.₅，输入观测+语言指令，输出 action chunk（flow-matching 策略） |
| **世界模型 M_φ**        | 基于 Ctrl-World，action-conditioned 视频扩散模型，预测未来 H 帧观测 |
| **奖励模型 R**          | 基于 Qwen3-VL-4B，判断轨迹是否完成任务（yes/no 概率阈值）    |
| **真实 rollout 收集器** | 每轮每任务类别收集 K=50 条真实轨迹，标注稀疏成功/失败标签    |
| **想象 rollout 生成器** | 策略与世界模型闭环交互，每任务生成 N=500 条合成轨迹          |
| **策略更新器**          | 对成功轨迹做加权 flow-matching 监督微调（不用 PPO/GRPO）     |

### 数据流

```
[预训练 VLA π₀.₅] + [预训练世界模型 Ctrl-World] + [VLM 奖励模型 Qwen3-VL]
        │
        ▼
(1) 真实世界 rollout → D_real（含成功+失败轨迹，稀疏奖励 r∈{0,1}）
        │
        ▼
(2) 微调世界模型：L = L_D_real + λ·L_D_DROID（防过拟合）
   微调奖励模型：用 D_real 的成功标签
        │
        ▼
(3) 想象 rollout：s₀~真实轨迹 → 闭环 [a_t~π, ŝ_{t+1}~M] → D_syn
   奖励过滤：R(τ)>α → D_syn⁺
        │
        ▼
(4) 策略微调：在 D_real⁺ ∪ D_syn⁺ 上做加权 flow-matching
        │
        └──→ 回到 (1)，迭代 K_iter 轮（实验中为 2 轮）
```

**输入：** 当前策略、世界模型、语言任务指令、少量真实 rollout 预算  
**输出：** 改进后的 VLA 策略 + 接地后的世界模型

---

## 三、关键公式解析

### 公式 (1)：世界模型扩散训练损失（D_real）

**原文：**
$$\mathcal{L}_{\mathcal{D}_{\mathrm{real}}}=\mathbb{E}_{x_{0},\,\epsilon,\,t^{\prime}}\left\|\hat{x}_{0}(x_{t^{\prime}},t^{\prime},c)-x_{0}\right\|^{2}$$

**符号对照表：**

| 符号        | 含义                        | 类型/维度          | 备注                                                         |
| ----------- | --------------------------- | ------------------ | ------------------------------------------------------------ |
| $x_0$       | 预测目标：未来 H 帧观测序列 | 视频 latent / 像素 | $x_0 = o_{t+1:t+H}$，从 D_real 采样                          |
| $x_{t'}$    | 加噪后的未来帧              | 同 $x_0$           | DDPM 前向扩散：$x_{t'}=\sqrt{\bar{\alpha}_{t'}}\,x_0+\sqrt{1-\bar{\alpha}_{t'}}\,\epsilon$ |
| $t'$        | 扩散时间步                  | 标量，$[0, T']$    | 训练时随机采样                                               |
| $\epsilon$  | 高斯噪声                    | 同 $x_0$           |                                                              |
| $c$         | 条件输入                    | 多模态             | 当前观测 $o_t$ + action chunk $a_{t:t+H}$                    |
| $\hat{x}_0$ | 模型预测的去噪结果          | 同 $x_0$           | Ctrl-World 的 x₀-prediction 形式                             |

**直觉解释：**
这就是标准的 **扩散模型去噪 MSE 损失**，但训练数据换成了策略在线 rollout（含失败案例），而不是纯演示数据。模型学习的是："给定当前画面和机器人动作，未来 H 帧画面应该长什么样"——包括物体碰撞、抓取失败等真实物理结果。

**容易误解的地方：**

1. 这不是在预测"成功/失败"标签，而是预测 **像素级未来视频**；成功与否由下游奖励模型判断
2. 条件 $c$ 包含 action chunk，所以这是 **action-conditioned** 世界模型，不是纯视频预测

---

### 公式 (2)：世界模型联合训练（防过拟合）

**原文：**
$$\mathcal{L}=\mathcal{L}_{\mathcal{D}_{\mathrm{real}}}+\lambda\,\mathcal{L}_{\mathcal{D}_{\mathrm{DROID}}}$$

**符号对照表：**

| 符号                                         | 含义                          | 类型/维度 | 备注                       |
| -------------------------------------------- | ----------------------------- | --------- | -------------------------- |
| $\mathcal{L}_{\mathcal{D}_{\mathrm{real}}}$  | 在线 rollout 上的扩散损失     | 标量      | 公式 (1)                   |
| $\mathcal{L}_{\mathcal{D}_{\mathrm{DROID}}}$ | 原始 DROID 数据集上的扩散损失 | 标量      | 同公式 (1)，数据来自演示集 |
| $\lambda$                                    | 正则化系数                    | 标量 > 0  | 控制对原始数据的保留程度   |

**直觉解释：**
在线 rollout 数据量小（每任务每轮仅 50 条），直接只用它会过拟合到当前策略的分布。混入大规模 DROID 演示数据相当于 **正则化**：既学到新任务的物理细节，又不丢掉通用机器人操作知识。

**容易误解的地方：**

1. $\lambda$ 不是"在线数据权重"，而是 **DROID 正则项的权重**；$\lambda$ 过大可能导致世界模型对目标任务接地不足
2. 每轮迭代 D_real 会累积增长（$\mathcal{D}_{\mathrm{real}} \leftarrow \mathcal{D}_{\mathrm{real}} \cup \tau_{\mathrm{real}}^i$），$\lambda$ 的相对效果会随迭代变化

---

### 公式 (3)：VLM 奖励模型阈值判定

**原文：**
$$R(\tau^{i})\;=\;\mathbf{1}\!\left[P(\texttt{`yes'}\mid\tau^{i},I^{i})>\alpha\right]$$

**符号对照表：**

| 符号                           | 含义                        | 类型/维度        | 备注                               |
| ------------------------------ | --------------------------- | ---------------- | ---------------------------------- |
| $\tau^i$                       | 第 i 条轨迹视频             | 16 帧下采样视频  | 从完整 rollout 时间下采样          |
| $I^i$                          | 任务语言指令                | 字符串           | 如 "stack red block on blue block" |
| $P(\texttt{`yes'} \mid \cdot)$ | VLM 输出 "yes" token 的概率 | 标量 $\in [0,1]$ | Qwen3-VL-4B 的 next-token 概率     |
| $\alpha$                       | 成功判定阈值                | 标量             | 实验中取 0.8（较保守）             |
| $R(\tau^i)$                    | 二值奖励                    | $\{0, 1\}$       | 1=成功，0=失败                     |

**直觉解释：**
不让 VLM 直接输出 yes/no 文本（容易过度乐观），而是看模型对 "yes" 这个 token 的 **置信度概率**。只有置信度超过阈值 α 才判为成功——相当于给自动判卷老师设了一个 **高门槛**，减少假阳性（把失败轨迹误判为成功）。

**容易误解的地方：**

1. 奖励是 **轨迹级稀疏奖励**（整条轨迹一个 0/1），不是逐步 dense reward
2. α=0.8 会显著降低 recall（真成功也被拒），但论文发现这对下游策略学习反而更好——因为假阳性的危害大于漏掉一些成功轨迹

---

### 公式 (4)：VLA 策略加权 Flow-Matching 损失

**原文：**
$$\mathcal{L}=\mathbb{E}_{(o,a)\sim\mathcal{D}_{\mathrm{syn}}^{+}\cup\mathcal{D}_{\mathrm{real}}^{+}}\,\mathcal{L}_{\mathrm{FM}}(\theta;o,a)$$

其中权重 $w(o,a)=1$ 对成功轨迹，$w(o,a)=0$ 对失败轨迹（等价写法见论文公式 4 第一行）。

**符号对照表：**

| 符号                                      | 含义               | 类型/维度          | 备注                                 |
| ----------------------------------------- | ------------------ | ------------------ | ------------------------------------ |
| $\mathcal{L}_{\mathrm{FM}}(\theta; o, a)$ | Flow-matching 损失 | 标量               | π₀.₅ 的原始训练目标                  |
| $(o, a)$                                  | 观测-动作对        | obs + action chunk | 从轨迹中拆出的 transition            |
| $\mathcal{D}_{\mathrm{real}}^{+}$         | 真实世界成功轨迹集 | 数据集             | 经奖励模型或人工标签过滤             |
| $\mathcal{D}_{\mathrm{syn}}^{+}$          | 想象世界成功轨迹集 | 数据集             | 经公式 (3) 过滤                      |
| $w(o,a)$                                  | 样本权重           | $\{0, 1\}$         | 失败轨迹权重为 0，等效于只用成功数据 |

**直觉解释：**
策略更新就是 **"只在成功轨迹上做行为克隆"**——但成功轨迹来源有两处：真实世界 + 世界模型想象。Flow-matching 策略没有显式 action 概率，所以不能用 PPO；作者选择最简单的加权监督学习，只在"做对了"的 (observation, action) 对上拟合。

**容易误解的地方：**

1. 这不是在线 RL（没有 bootstrapping / advantage 估计），而是 **离线加权 BC**
2. 合成数据和真实数据 **同等对待**（都是 w=1），没有额外加权——消融实验表明去掉真实数据会显著降性能

---

### 公式 (5)：正则化 RL 目标

**原文：**
$$J(\theta)=\mathbb{E}_{\tau\sim\rho_{\pi_{\theta}}}\!\left[R(\tau)\right]\;-\;\beta\,\mathbb{E}_{o\sim\rho_{\pi_{\theta}}}\!\left[D\!\left(\pi_{\theta}(\cdot\mid o)\,\|\,\pi_{\mathrm{ref}}(\cdot\mid o)\right)\right]$$

**符号对照表：**

| 符号                 | 含义                    | 类型/维度 | 备注                  |
| -------------------- | ----------------------- | --------- | --------------------- |
| $J(\theta)$          | 正则化 RL 目标函数      | 标量      | 最大化                |
| $R(\tau)$            | 轨迹级奖励              | 标量      | 成功=1，失败=0        |
| $\pi_{\mathrm{ref}}$ | 参考策略                | 策略分布  | 通常是当前/上一轮策略 |
| $D(\cdot \| \cdot)$  | 散度度量                | 标量      | 标准 KL 散度          |
| $\beta$              | 正则化温度              | 标量 > 0  | 越大越贴近参考策略    |
| $\rho_{\pi_\theta}$  | 策略诱导的状态/轨迹分布 | 分布      |                       |

**直觉解释：**
这是 **"在追求高奖励的同时不要偏离原策略太远"** 的经典正则化 RL 框架（类似 TRPO/PPO 的 trust region 思想）。论文用这个框架为公式 (4) 的加权 BC 提供理论解释——说明简单的成功轨迹过滤 + 监督学习，可以近似看作在最优改进策略上的投影。

---

### 公式 (6)/(8)：最优改进策略的闭式解

**原文：**
$$\pi^{\star}(a\mid o)\propto w(o,a)\,\pi_{\mathrm{ref}}(a\mid o),\quad w(o,a)=\exp\!\left(\frac{A^{\pi_{\mathrm{ref}}}(o,a)}{\beta}\right)$$

**符号对照表：**

| 符号                          | 含义         | 类型/维度 | 备注                                                      |
| ----------------------------- | ------------ | --------- | --------------------------------------------------------- |
| $\pi^*(a \mid o)$             | 最优改进策略 | 动作分布  | 闭式解                                                    |
| $A^{\pi_{\mathrm{ref}}}(o,a)$ | 优势函数     | 标量      | $Q^{\pi_{\mathrm{ref}}}(o,a) - V^{\pi_{\mathrm{ref}}}(o)$ |
| $w(o,a)$                      | 指数优势权重 | 标量 > 0  | 高优势动作被放大                                          |
| $\beta$                       | 温度参数     | 标量      | 控制探索程度                                              |

**直觉解释：**
在正则化 RL 下，最优策略就是 **"参考策略 × 优势加权"**——优势高的 (obs, action) 对在改进策略中获得更高概率。当 $\gamma \to 1$ 且给失败轨迹大负奖励时，这个权重退化为二值：成功轨迹 $w=1$，失败轨迹 $w \approx 0$，正好对应公式 (4) 的做法。

**容易误解的地方：**

1. VLAW **没有显式计算优势函数**，而是直接用二值成功标签近似——这是一个有意的简化，依赖奖励模型的准确性
2. 闭式解 $\pi^*$ 一般无法被参数化策略精确表示，所以需要投影步骤（公式 7）

---

### 公式 (7)/(10)/(11)：Flow-Matching 投影散度

**原文：**
$$D_{\mathrm{FM}}\!\left(\pi^{\star}(\cdot\mid o),\pi_{\theta}(\cdot\mid o)\right)\;\triangleq\;\mathbb{E}_{a\sim\pi^{\star}(\cdot\mid o)}\big[\mathcal{L}_{\mathrm{FM}}(\theta;o,a)\big]$$

$$\theta^{\star}=\arg\min_{\theta}\;\mathbb{E}_{(o,a)\sim\mathcal{D}}\Big[w(o,a)\,\mathcal{L}_{\mathrm{FM}}(\theta;o,a)\Big]$$

**符号对照表：**

| 符号                                      | 含义                      | 类型/维度 | 备注                   |
| ----------------------------------------- | ------------------------- | --------- | ---------------------- |
| $D_{\mathrm{FM}}$                         | Flow-matching 代理散度    | 标量      | 替代不可计算的 KL 散度 |
| $\mathcal{L}_{\mathrm{FM}}(\theta; o, a)$ | 单样本 flow-matching 损失 | 标量      | π₀.₅ 原生训练目标      |
| $\theta^*$                                | 最优投影参数              | 模型参数  | 加权回归的解           |
| $\mathcal{D}$                             | 固定数据集                | 数据集    | 真实 + 合成 rollout    |

**直觉解释：**
Flow-matching 策略没有 action log-probability，所以不能用标准 AWR 的加权 log-likelihood。作者定义了一个 **代理散度**：用 flow-matching loss 衡量策略对目标分布的拟合程度。投影步骤就变成了公式 (4) 的加权 flow-matching 回归——这是 VLAW 能用监督学习替代 PPO 的关键理论桥梁。

**容易误解的地方：**

1. $D_{\mathrm{FM}}$ 不是严格的 statistical divergence（不满足非负性等公理），是 **工程可用的代理度量**
2. 从 $\mathbb{E}_{a \sim \pi^*}[\mathcal{L}_{\mathrm{FM}}]$ 到 $\mathbb{E}_{(o,a) \sim \mathcal{D}}[w \cdot \mathcal{L}_{\mathrm{FM}}]$ 的近似，假设数据集覆盖了 $\pi^*$ 的支撑——如果奖励模型有大量假阳性，这个假设会被破坏

---

## 四、技术优势

1. **用在线 rollout 解决世界模型"过度乐观"问题**
   - 仅用专家演示微调的世界模型 FP（假阳性）高达 11/30；加入在线 rollout（含失败）后 FP 降至 1/30
   - 对世界模型基座：说明 **训练数据分布（成功 vs 失败比例）** 比模型容量更影响物理保真度

2. **监督学习替代 policy gradient，兼容 flow-matching VLA**
   - π₀.₅ 等 SOTA VLA 用 flow-matching 训练，无法直接算 action log-prob，PPO/GRPO 不适用
   - VLAW 用加权 BC + FM 代理散度，训练稳定、易扩展到 3B 级模型

3. **世界模型作为可扩展数据生成器**
   - 每任务 50 条真实 rollout → 500 条合成 rollout（10× 放大），且合成数据收集成本接近零
   - 在接触丰富任务（scooping、drawing）上，合成数据带来 11.6% 绝对成功率提升

4. **迭代共进化框架简单可复现**
   - 整个 pipeline 只需 4 步（收集→微调 WM→生成→微调 policy），Algorithm 1 可直接翻译为训练脚本
   - 2 轮迭代即可从 46% 平均成功率提升到 86.8%

5. **VLM 奖励模型降低人工标注成本**
   - 用 Qwen3-VL-4B 替代人工判分，仅需 200 步微调 + 概率阈值（α=0.8）
   - 假阳性从 8 降至 2（40 条人工标注子集上验证）

---

## 五、局限性与潜在缺陷

### 论文自述局限
- 仅在 **5 类接触丰富任务** 上验证，未证明跨域泛化
- 世界模型高保真度 **局限于收集过在线数据的下游任务**
- 扩展到更多样任务集是未来方向

### 工程推断的潜在问题

1. **[推断] 想象 rollout 的误差累积**
   - 自回归生成 20 秒（20 步）后视觉保真度会下降；论文只展示了 20 步的定性结果，更长 horizon 的可靠性未知
   - 对基座世界模型：需要监控 **闭环 rollout 长度 vs 预测质量** 的衰减曲线
2. **[推断] 奖励模型是系统瓶颈**
   - 阈值 α=0.8 使 recall 降低（真成功 22 中只识别 10 个），但 precision 提高
   - 假阳性合成数据会直接污染策略训练——奖励模型错误比世界模型错误更致命
3. **[推断] 世界模型仅预测视觉观测，不预测力/触觉**
   - 接触丰富任务的关键信息（接触力、形变内部状态）不在视频预测中
   - 对需要力控的任务，纯视觉世界模型可能有天花板
4. **[推断] 合成数据与真实数据的分布 gap**
   - 消融显示去掉真实成功轨迹后性能显著下降——说明合成数据 **不能替代** 真实数据，只能补充
   - 世界模型生成的"成功"轨迹可能在视觉上合理但物理上 subtly wrong

### 对世界模型基座场景的具体挑战
- Ctrl-World 依赖 DROID 数据预训练，迁移到非 DROID 平台（如 VITRA 的人手数据）需要重新接地
- 多任务共训练时 $\lambda$ 的选择缺乏理论指导，可能需要 per-domain 调参
- 缺乏对世界模型 **不确定性估计** 的机制——无法判断何时想象 rollout 不可信

---

## 六、对团队的启发（最重要）

### 最值得复现/借鉴的技术点

**第一名：用在线 rollout（含失败）微调世界模型的 co-training 策略（公式 2）**

这是 VLAW 最核心的发现——不是换一个更大的视频模型，而是 **换训练数据分布**（加入策略执行中的失败轨迹），就能让世界模型从"过度乐观"变为"物理接地"。对团队的世界模型基座：

- 在预训练阶段就应规划 **失败案例数据通道**，不能只收集演示
- co-training 正则化（预训练大数据集 + 小量在线数据）是防止灾难性遗忘的实用方案

**第二名：VLM 奖励模型 + 保守阈值（公式 3）作为世界模型 rollout 的质量门控**

团队可以用类似机制做世界模型生成数据的 **自动质检**，不必依赖人工标注每条合成轨迹。

**第三名：加权 FM 回归作为 flow-matching 策略的 RL 替代（公式 4/7）**

如果团队基座策略采用 flow-matching / diffusion policy，VLAW 提供了比 PPO 更实用的后训练路径。

### 对基座架构选型的影响

| 设计决策          | VLAW 的启示                                                  |
| ----------------- | ------------------------------------------------------------ |
| 世界模型输出模态  | 视觉观测（视频帧）足够驱动策略改进，但可能需要补充力/状态预测 |
| 世界模型训练数据  | 必须包含 **策略诱导的分布**（online rollout），纯演示数据不够 |
| 策略-世界模型耦合 | 迭代共进化优于一次性训练；架构上应支持 **独立微调 + 闭环交互** |
| 奖励信号          | VLM 轨迹级二值奖励可行，但需保守阈值控制假阳性               |
| 与 VITRA 的关系   | VITRA 提供人类视频预训练的 VLA 冷启动 → VLAW 提供部署后的迭代改进；两者可串联 |

### 第一步实验建议

1. **复现世界模型接地实验**（不需要完整 RL 循环）：
   - 拿现有世界模型基座，收集 50 条策略 rollout（含失败）
   - 对比三种微调：仅 DROID / 仅专家演示 / 在线 rollout + DROID co-training
   - 用 action replay 指标（PSNR、SSIM、交互事件混淆矩阵）验证接地效果

2. **建立 action replay 评测基准**：
   - 从真实轨迹随机取起始帧 + 回放 5 秒 action sequence
   - 对比预测视频与 GT 的像素指标 + 交互事件成功率
   - 这是比 FVD 更能反映物理保真度的工程指标

3. **测试 VLM 奖励模型**：
   - 用 Qwen3-VL 或团队已有 VLM，在 40 条轨迹上对比直接 yes/no vs 概率阈值
   - 确定适合团队任务的 α 值

### 论文没明说但大概率会踩的坑

1. **想象 rollout 的起始状态必须从真实轨迹采样**——论文从真实帧 $s_0$ 开始想象，随机初始化会导致世界模型分布外
2. **世界模型微调步数（50K）远超策略微调（2K）**——WM 是瓶颈模块，省 WM 训练会得不偿失
3. **多任务联合训练时任务间干扰**：5 类任务共享一个世界模型，未见任务可能性能下降
4. **手腕相机视角最关键**——论文所有视频质量指标只用 wrist camera，第三人称相机对接触建模贡献小
5. **奖励模型只需在第一轮迭代微调**——后续轮次复用，但策略分布漂移后奖励模型可能需重新校准（论文未验证）
6. **与 VITRA 集成的坐标系对齐**：VITRA 用 MANO 人手表示 + 相机坐标系，VLAW 用 DROID EEF + 三相机；若串联使用需处理 action space 和 observation space 的统一

---

## 七、相关资源

### 关键前置工作
- **π₀.₅** — VLA 基座策略（flow-matching）
- **Ctrl-World** — action-conditioned 视频扩散世界模型
- **DROID** — 大规模真实机器人操作数据集
- **Qwen3-VL / RoboReward** — 通用 VLM 奖励模型
- **Advantage-Weighted Regression (AWR)** — 正则化 RL 理论基础
- **Stable Video Diffusion** — 视频扩散模型基础

### 值得对比阅读的相关工作
- **π*₀.₆** — 同类 VLA 在线改进，但用 advantage-conditioned SFT 而非世界模型
- **World4RL** — 同样用扩散世界模型做策略精炼，但在仿真中验证
- **WMPO** — World Model-based Policy Optimization for VLA
- **DayDreamer / SOLAR** — 真实世界视觉 model-based RL 早期工作
- **VLA-RFT** — 在 world simulator 中用 verified rewards 做 RL fine-tuning
- **VITRA**（本仓库）— 人类视频预训练 VLA，解决 complementary 的冷启动问题
- **1X World Model / Gemini Robotics VEo** — 工业界世界模型评估实践

### 开源代码
- 论文项目页：[https://sites.google.com/view/vlaw-arxiv](https://sites.google.com/view/vlaw-arxiv)
- 基座模型：**Ctrl-World**（世界模型）、**π₀.₅**（VLA 策略）
- 数据集：**DROID**
- 本仓库 **VITRA**（[microsoft/VITRA](https://github.com/microsoft/VITRA)）— 人类手部 VLA 预训练，可作为 VLAW 框架的策略冷启动替代

Copyright © 2026 [Austin-152](https://github.com/Austin-152/). All rights reserved.