# 世界模型调研汇总 \-\- Austin

## 执行摘要  

随着**Vision–Language–Action \(VLA\)** 模型在机器人控制领域的迅速发展，产品导向企业在选择具身智能机器人系统时需综合考量模型能力、世界模型、记忆机制、训练策略与工程落地等多维度因素。本报告首先对**机器人模型**（VLA/RT\-X/OpenVLA/Octo/RT\-2等及其骨干网如DINOv2/DINOv3/CLIP的利弊）和**世界模型**（潜在动态模型如DreamerV3/TD\-MPC2，扩散模型如CTRL\-World，3D 点云/场景图/NeRF/高斯点云的优缺点）进行详细比较；然后讨论**记忆模块**（情景记忆、场景图、三维地图）的特点；接着分析**训练与优化策略**（分阶段训练、想象重放、自监督RL及其风险与缓解）和**工程可落地性**（数据需求、仿真到现实转移、算力与复杂度评估、风险矩阵）；最后提出**两套可执行方案**（中性、激进），并用表格和流程图加以对比说明。每套方案列出组件清单、训练流程、数据量、算力预估、里程碑、风险与缓解、性能预期与验收标准。报告内容引用了包括国际顶级会议/期刊论文和工程实现文档等权威资料，兼顾前沿性与可行性，力求对产品决策和项目规划提供全面指导。



## 论文列表：

### [Point World](https://qcnxc2ih90pn.feishu.cn/wiki/Gv8OwF8Luin4xgkAFt3cTQzJn9c) : 

采用transformer架构对物体的性质（柔性/刚性等）具有一定的预测能力的世界模型，具有一定程度的可泛化性。

但目前仍 需要 **手动指定** 任务点和目标位置；且**薄/小物体（笔、线）3D 标注困难**。


### [VLAW](https://qcnxc2ih90pn.feishu.cn/wiki/WO3xwXX8XibmvdkOohncIx2an1b) ：

论文提出一种迭代协同改进框架：用少量真实 rollout 微调世界模型，再用微调后的世界模型在"想象"中生成大规模合成轨迹，配合 VLM 奖励模型筛选成功轨迹，以监督式 flow\-matching 目标改进 VLA 策略。

但论文中给出的vla模型仅在 **5 类接触丰富任务** 上验证，**未证明跨域泛化能力**。



### [OpenVLA ](https://qcnxc2ih90pn.feishu.cn/wiki/Z6hDwJ11iiOM4vkyJmEclfVlnUf)：

让机器人策略模型具备 Internet\-scale 视觉\-语言泛化能力，同时以开源、可微调的方式供社区使用（现有 VLA（如 RT\-2\-X）虽性能强劲，但模型闭源、训练细节不透明）。

但局限是**单帧输入**：不支持多相机、proprioceptive state、观测历史；且推理速度为\~6Hz（RTX 4090），不支持 50Hz 高频控制（如 ALOHA）；同时无 action chunking



## 机器人模型对比  

- **VLA模型**：Vision–Language–Action模型直接将视觉输入和自然语言指令映射到机器人动作。典型代表有**RT\-2**（Google DeepMind提出）、**RT\-X**（在多机器人数据上训练的RT系列扩展）、**OpenVLA**（开放源代码，7B参数，使用DINOv2\+SigLIP编码）、**Octo**（伯克利团队开源，使用Diffusion Policy） 等。OpenVLA 在 Open\-X Embodiment（OXE）数据集上微调后，表现优于RT\-2和Octo；Octo 利用T5与CNN编码视觉、语言信息，并通过扩散头生成连续动作。RT\-2 通过将机器人动作编码为语言 token，与互联网上视觉\-语言数据联合微调，可获得显著的泛化和推理能力。RT\-X 表明在多机器人数据上训练（22种机器人、100万\+轨迹）可提升模型跨平台泛化性（RT\-1\-X在五种机器人上成功率提高\~50%，RT\-2\-X性能近似提升3倍）。

- **骨干网络 \(Backbone\)**：VLA通常采用大规模视觉语言模型作为骨干，可选项包括 **DINOv2/v3** 和 **CLIP** 系列。中OpenVLA采用DINOv2\+SigLIP进行视觉编码，并以LLaMA\-7B为语言骨干；最新研究发现**CLIP**也能作为VLA骨干：例如CLIP\-RT（1B参数）在家用物体操作任务上的新任务成功率分别比OpenVLA（7B参数、DINOv2骨干）高3%（常见任务）和24%（新任务）。CLIP\-RT速度快（在H100卡上可达16Hz推理）且参数量较小。DINOv3作为DINO系列最新版本，可能提供更强的视觉表示，但目前应用实例较少（相比DINOv2需更多验证）。总的来说，CLIP骨干可利用大量图像\-文本预训练知识，但在机器人视觉细节方面可能略逊于专门的视觉自监督（如DINO）；DINOv2/v3为视觉模型，缺少语言预训练。工程上可考虑**Fusion**：如OpenVLA同时使用DINOv2和SigLIP融合多视角信息。

- **动作表示**：VLA有三种常见动作建模方法：**离散动作 token** 、 **扩散生成连续动作和条件流匹配（Conditional Flow Matching, CFM）**。OpenVLA、RT\-2系列等常将固定离散动作编码为token（如旋转、夹爪开闭等），训练中使用交叉熵自回归生成；Octo等扩散模型则在Transformer后接扩散头，直接生成连续动作矢量。LearnOpenCV指出，Octo的扩散头在处理动作时比离散token效果更好。离散表示易于训练和部署，但需预定义动作字典；扩散生成灵活连续，但计算复杂度高、需要大量数据训练。条件流匹配（Conditional Flow Matching, CFM）最前沿，作为$\pi_0$ 等新一代模型的标配动作头，兼具扩散模型生成连续复杂轨迹的流畅度，同时大幅降低了采样迭代步数，是实现高频（\>30Hz）闭环控制的工程首选。

## 世界模型对比  

机器人系统中的**世界模型**指预测环境未来状态的模型，常用于**样本效率提升和想象训练**。主要类别及特点：

- **潜在动态模型 \(Latent Dynamics\)**：如**DreamerV3**、**TD\-MPC2**等，这类模型在隐空间中学习动力学并进行规划，通常样本效率高。DreamerV3作为第三代Dreamer算法，在150多个任务上以单一配置超越专门算法；TD\-MPC2在104个连续控制任务上表现出一致良好的效果（使用一个模型处理多个任务和动作空间）。此类模型直接学习隐空间转移，不需建模像素细节，易于融入RL优化。优点是利用了学到的隐空间表示，通常比像素级生成更节省资源；缺点是需要稳定的世界模型训练和很好的编码器设计。

- **扩散/生成式模型 \(Generative World Models\)**：例如**CTRL\-World**使用视频扩散模型生成未来视频预测，可用于评估和改进策略。CTRL\-World利用预训练的视频扩散模型，通过多视角预测、帧级动作条件和记忆检索机制，实现了**20秒以上**的长期一致性生成。论文演示该模型能在想象空间中评估机器人策略（与真实数据对齐）并生成纠正性轨迹用于微调，显著提高策略成功率约44\.7%。扩散模型能生成高保真的视觉场景，对于评估开放世界任务有优势，但训练复杂、资源消耗大；生成结果可能存在“幻觉”（hallucination），需要策略来约束（参见下文风险讨论）。此外，视频扩散通常需要大量视频数据预训练，再通过动作条件和短期记忆等机制适配机器人交互。

- **三维/点云/场景表示**：包括**点云/体素地图**、**场景图 \(Scene Graph\)**、**神经辐射场 \(NeRF\)**、**高斯点云 \(Gaussian Splatting\)**等。三维场景模型具有结构化和物理信息但各有利弊：场景图可表示物体语义和关系，便于语义推理和问答；但构建和更新成本较高，依赖物体检测和分割。3D地图（体素、点云、TSDF）对导航和避障非常有效，**但缺乏语义信息**。**NeRF**代表隐式3D场景，高质量可视化的同时可用于视觉闭环控制；最新技术（Instant\-NGP）已将NeRF训练和渲染加速到可交互级别。**3D Gaussian Splatting \(3DGS\)** 则通过大量各向异性高斯基元显式建模场景，具有极快的渲染速度和高保真度。**高斯世界模型 \(GWM\)**是将3D高斯点云嵌入世界模型的示例，显著提升了对未来场景的预测精度：论文显示GWM在多环境下的动作条件预测上超过了基于图像的iVideoGPT，并能用生成的轨迹训练更优策略。综上，选择何种3D表示依赖于任务：若注重精确动作控制和物理一致性，**高质量三维模型（NeRF/3DGS）**是优势，但实时性和动态更新是挑战；若关注语义推理，可考虑**场景图**结合3D信息。

## 记忆模块  

机器人长期任务中，记忆模块用于存储过去信息以支持规划与问答。常见策略包括**情景记忆 \(Episodic Memory\)**、**场景图/图谱记忆**和**三维地图**等：

- **情景记忆**：记录特定事件或时间点的信息，适用于回答“发生了什么”或“哪里发生了”类问题。最新研究提出基于贝叶斯惊奇度（Bayesian Surprise）触发记忆存储，以筛选高价值事件。该文中使用V\-JEPA\-2潜空间的预测误差作为“惊奇度”准则，将重要经验存入情景记忆，并用它增强**4D场景图**的空间记忆，显著提升机器人问答和事件分割性能。情景记忆的优点是保留关键记忆以辅助长期规划；缺点是如何高效索引和管理大量事件数据。

- **场景图 \(Scene Graph\)**：通过节点\-边结构表示环境中物体、类别和它们的空间/语义关系。动态**3D场景图**（如Kimera动态场景图）结合SLAM与语义信息，可实时更新多层次地图（物体层、房间层等）。场景图可直接支持自然语言定位和查询（“水杯在哪个桌子上？”）。构建场景图需要物体检测、跟踪与关系推理，目前已有SGformer等方法用点云直接推断场景图。其主要局限在于：依赖昂贵的感知算法，难以在动态场景下及时更新，且初期构建复杂。  

- **三维地图**：指点云、体素网格或TSDF等形式的环境几何模型，通常用于导航。与场景图相比，3D地图关注占用空间信息，可提供稳定的几何基准（如避障和定位），但缺乏高层语义。经典SLAM系统（如Kimera）可生成三维地图并附加语义标签。三维地图便于保持对环境结构的长期记忆，但对推理和语言指导的支持有限。

**比较**：情景记忆提供语义丰富的历史片段，适合需要“回忆”式任务；场景图融合几何与语义，可做语言导航和复杂查询；3D地图则保证了稳定的空间理解和规划基础。实际系统可将多者结合：例如用场景图维护环境状态，用惊奇度触发的情景记忆来补充动态事件记录；并持有基础的三维地图作参考。  

## 训练与优化策略  

- **分阶段训练 \(Staged Training\)**：通常先进行**模仿学习**（行为克隆）以教会VLA模型基础任务，然后可将其作为基础策略或数据生成器，进一步进行强化学习优化或其他算法调优。比如，可先用真实/仿真的演示数据训练VLA，然后**冻结视觉语言模型**，仅训练世界模型或控制层，再通过世界模型生成经验来细调策略。分阶段方法风险较低，可逐个环节定位问题，但耗时较长，需要更多数据和迭代。

- **想象重放 \(Imagination Replay\)**：结合世界模型进行“心智模拟”训练，例如在世界模型内生成未来轨迹作为额外数据。Dreamer系列通过在隐空间做想象规划，显著提高样本效率；CTRL\-World提出使用合成成功轨迹来监督微调策略，可提升实际成功率。我们可采用类似策略：训练稳健的世界模型后，让当前策略在其内部滚动出场景，然后将生成的（成功或失败）轨迹混入训练集中。风险是**模型幻觉**（world model预测偏差），可能引导策略学习错误行为，需要加以缓解（见下文）。

- **联合训练风险与缓解**：直接将VLA策略与世界模型或其他模块同时训练会带来不稳定性。WoVR工作指出，封闭循环地在学习的世界模型中训练RL容易导致累积误差和“幻觉”。为此，可采用三项关键机制：1\) **行动条件世界模型**（action\-conditioned video model），保证生成对实际动作的响应；2\) **关键帧初始化回放**，限制想象长度，定期“重置”真实数据以降低误差扩散；3\) **策略与世界模型共进化**（Policy\-Aligned Co\-Evolution），让模型根据策略改变而动态更新。经过这些处理后，WoVR在模拟器中将LIBERO任务成功率从39\.9%提升到69\.2%，在真实机器人上从61\.7%提升到91\.7%，表明合理控制幻觉可实现稳定提升。因此，我们在实际方案中可借鉴WoVR框架，对想象训练进行**有意识的约束**，以避免误导。与此同时引入 **VLAW（视频世界模型与VLA策略的迭代协同改进框架）**。利用少量真实世界 rollout 微调增强世界模型的物理逼真度，随后在离线‘想象空间’中进行低成本的大规模合成轨迹扩展；同时引入强大的全尺寸 VLM 作为自动化奖励模型（Reward Model）对合成轨迹进行成败筛选，最终以监督式 Flow\-matching 目标对 VLA 策略进行蒸馏训练。该机制将‘防幻觉’从被动截断升级为主动的数据质量对齐飞轮。

- **数据增强与规约**：结合**人类视频**（如VITRA方法）进行预训练，可极大扩充VLA训练集；同时可进行域随机化、图像扰动等，以提高策略的泛化能力。**动作序列标记化**也是一大实践要点——上述VITRA对动作进行了**因果注意力**和**相机内参补偿**等优化，提示我们在构建模型时要考虑输入的顺序偏差和尺度变换。

## 工程可落地性  

- **数据需求**：VLA训练需要大量多模态数据。Open X\-Embodiment 数据集（22种机器人、100万轨迹）为实例；VITRA工艺自动构建了1M\+人类视角动作片段（30M帧）。保守方案可主要依赖现有仿真或公开示例（\<10万条）；进阶方案需采集数百万级真实示例（含语言注释）。**Sim\-to\-Real**：模拟数据（如**NVIDIA Isaac Sim / Isaac Lab \(Omniverse\)** 或 **RoboTwin 2\.0**）能快速收集多样数据，但与真实世界存在差距，建议结合域随机化和视觉对齐技术减小差异。

- **算力估算**：大规模VLA和世界模型训练需要GPU集群支持。OpenVLA指出使用7B模型参数，通过LoRA微调和量化可在消费级GPU上进行训练。CLIP\-RT仅1B参数，也可在单卡实时推理。若采用DreamerV3/TD\-MPC2等大模型，或者训练视频扩散世界模型，需要多卡并行训练（如多张A100）。保守方案（小模型）可用1\~2张GPU；中性方案可能需要4\~8卡；激进方案（大VLA\+扩散模型\+大量数据）可能需几十卡或更大型集群。训练时间也将从数月（方案A）扩展到半年甚至一年以上（方案C）。

- **实现复杂度**：实现难度随方案激进程度增加。保守方案主要基于已有开源代码和库，难度低；激进方案需自研大规模数据标注流水线（如3D重建、自动分割、语言生成）、大型模型训练、软硬件工程量都大。此外，仿真\-现实迁移和长期部署测试也是挑战。需构建**风险矩阵**：例如数据质量风险（数据偏差导致泛化差），训练风险（模型崩溃）、部署安全风险（物理干预）等，并制定相应缓解，如冗余检测、在线学习、安全监控。

- **评估与验证**：主要指标包括**泛化能力**（新对象、新环境下成功率）、**长期规划能力**（多步骤任务成功率）、**样本效率**（每次数据带来的性能提升）、**实时性**（控制闭环频率）。评估方案可以采用标准任务集（如RLBench/LIBERO）和自行设计的长期任务序列；同时通过仿真和现实双向验证确保策略的可转移性。

## 关键组件与方案比较  

## 训练与部署流程示意  



- **流程说明**：首先**收集数据**（真实示范、仿真、可能还有人类视频），进行VLA模型的**模仿学习预训练**；然后可**冻结VLA的视觉语言部分**，训练**世界模型**（latent或扩散）以学会环境预测；接着在世界模型内进行**想象式模拟**（用当前策略滚动出样本轨迹），将这些轨迹与真实数据一起用于**策略微调**（可采用行为克隆或安全RL）；最后将模型**部署到机器人上**，并持续监测和**在线微调**以应对现实偏差。

## 方案细节说明  

### 方案A：中性路线  

- **组件清单**：  

    - *机器人模型*：OpenVLA\-7B（DINOv2\+SigLIP骨干）或CLIP\-RT。  

    - *世界模型*：基于潜在动力学的模型（例如DreamerV3、TD\-MPC2）用于想象训练。  

    - *记忆模块*：3D场景图 \+ 基本情景记忆触发（如定期存储关键帧）。  

- **训练流程**：  

    1. 使用现有数据（如OXE多机器人数据）对VLA模型进行**初始微调**。  

    2. 冻结VLA视觉层，训练世界模型（隐空间预测）。  

    3. 在世界模型中进行**想象重放**：生成新的轨迹用于进一步**监督微调**VLA策略。  

    4. 混合真实与合成数据，进行少量**强化学习微调**（参考WoVR等手段控制风险）。  

    5. 连续验证模型性能，更新数据集。  

- **数据集与数据量**：  

    - 集成多机器人示范数据 \~**100万**轨迹（如Open X\-Embodiment），并根据任务需要添加高质量模拟数据。  

    - 可引入*少量*人类视频数据（如VITRA流水线产出）进行预训练。  

- **算力与基础设施**：  

    - 需要4–8张高端GPU（如A100/H100）并行训练。  

    - VLA微调与世界模型训练各占一部分资源；想象训练增加额外计算开销。  

- **里程碑**：  

    - **1**：数据收集与预处理（多机器人示范、多视角视频转化）。  

    - **2**：VLA模型微调训练；世界模型初步训练。  

    - **3**：想象数据生成与策略微调；中期验收（基于仿真）。  

    - **4**：综合测试与部署准备；实机迭代改进。  

- **主要风险与缓解**：  

    - *风险*：世界模型生成不精准导致策略误导；训练复杂度上升。  

    - *缓解*：借鉴WoVR设计（关键帧初始化、多模型更新），定期用真实数据校正；采用LoRA/量化减少VLA训练成本。  

- **预期性能与验收标准**：  

    - 复杂环境中新任务成功率达到50–70%。  

    - 长时任务中，终止精度与成功率显著超过方案A。  

    - 控制系统闭环频率达到5–10Hz，满足实时需求。  

### 方案B：激进路线  

- **组件清单**：  

    - *机器人模型*：大型VLA模型（如OpenVLA\-14B或多模型融合，可能用DINOv3\+CLIP）。  

    - *世界模型*：融合多视角**CTRL\-World**视频扩散模型，结合3D高斯场景（GWM）来预测未来空间。  

    - *记忆模块*：完整4D场景图＋*惊奇度触发*的情景记忆系统。  

- **训练流程**：  

    1. **大规模预训练**：使用VITRA流水线自动生成的人类动作视频数据集（百万\+实例），对VLA模型进行预训练；再用多机器人真实数据微调。  

    2. 训练**扩散世界模型**：在大规模视频数据（如DROID、合成数据）上预训练CTRL\-World网络，使之能生成长时多视图视频。  

    3. 训练**高斯世界模型**：利用3D扫描或多视角视频构建GWM，以在三维层面预测物体运动。  

    4. **联合优化**：结合VLA、CTRL\-World、GWM进行**想象式RL**训练，采取WoVR等严谨机制控制误差。  

    5. **多模型集成**：形成层次化架构（如RT\-H思路），高层使用VLA执行语言计划，低层使用世界模型细化控制。  

- **数据集与数据量**：  

    - 汇集**10M\+**高质量示范和视频数据：包括抓取视频（VITRA）、仿真增强、公共场景数据集（Ego4D, Epic\-Kitchens）等。  

    - 构建大规模语义标签（动作描述、视觉重投影、场景分割）以辅助学习。  

- **算力与基础设施**：  

    - 大型训练集群（如上百个GPU/TPU）分阶段并行：包括VLA预训练、视频扩散训练、3D场景模型训练、RL微调。  

    - 系统需大量存储和网络带宽来处理视频和点云数据。  

- **里程碑**：  

    - **1**：数据管线建设（自动化视频转示范、场景重建）。  

    - **2**：VLA 和 世界模型大规模训练（可并行开发）。  

    - **3**：联合训练与评估，模型融合与层次化策略开发。  

    - **4**：系统集成、实地测试和安全验证；准备量产部署。  

- **主要风险与缓解**：  

    - *风险*：项目周期长、需求大量资源；系统过于复杂导致可靠性问题。  

    - *缓解*：阶段性发布可工作的子系统；建立严格的测试流水线；重点监控安全和失败模式；保留功能退回方案。  

- **预期性能与验收标准**：  

    - 在未见环境和长序列任务上成功率达到80%以上。  

    - 支持多机器人平台部署（跨硬件转移能力强）。  

    - 实时性目标\>10Hz，能处理高级指令（如多步推理）并表现鲁棒。  

**参考文献：** 报告内容参考了包括RT\-2/RT\-X/OpenVLA/Octo等论文、世界模型与扩散模型论文、记忆与训练方法研究等，及相关工程实现和博客资料。这些参考资料为制定方案提供了理论与实践依据。



Copyright © 2026 [Austin-152](https://github.com/Austin-152)\. All rights reserved\.

